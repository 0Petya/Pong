direction = irandom(360)
speed = irandom(10)

while (speed < 5)
    speed = irandom(10)

while ((direction > 120 && direction < 60) || (direction > 240 && direction < 300))
    direction = irandom(360)

